%section.content-header
  %h1
    - if policy(:case_status).new?
      = link_to new_case_status_path do
        %button.btn.btn-primary
          Nuevo
  %ol.breadcrumb
    %li
      %a{:href => "#"}
        %i.fa.fa-dashboard
        Estado del caso
    %li.active Listado

.container-fluid.mt-10
  .row
    .col-lg-12
      .box.slice-box
        .box-body.no-padding
          %table.table
            %thead
              %tr
                %th #
                %th Nombre
                %th
                %th
            %tbody
              = render 'listing'
        .box-footer.text-center
          = paginate @case_statuses, theme: 'bootstrap', window: 3
