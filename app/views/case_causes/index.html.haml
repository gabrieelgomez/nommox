%section.content-header
  %h1
    - if policy(:case_cause).new?
      = link_to new_case_cause_path do
        %button.btn.btn-primary
          Nuevo
  %ol.breadcrumb
    %li
      %a{:href => "#"}
        %i.fa.fa-dashboard
        Causas de casos
    %li.active Listado

.container-fluid.mt-10
  .row
    .col-lg-12
      .box.slice-box
        .box-body.no-padding
          %table.table
            %thead
              %tr
                %th #
                %th Nombre
                %th
                %th
            %tbody
              = render 'listing'
        .box-footer.text-center
          = paginate @case_causes, theme: 'bootstrap', window: 3
